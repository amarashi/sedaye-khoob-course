{
  "name": "sedaye-khoob-course",
  "version": "1.0.0",
  "private": true,
  "description": "Customer-ready Sedaye Khoob course website with JSON-managed content, checkout, payment, order storage, and Spot Player licence automation.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check server/index.js",
    "validate": "node --check server/index.js && node --check server/content-renderer.js && node -e \"JSON.parse(require('fs').readFileSync('content/site.json', 'utf8')); console.log('content/site.json is valid JSON')\"",
    "build": "npm run validate"
  },
  "engines": {
    "node": ">=18 <24"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemailer": "^8.0.10"
  }
}
